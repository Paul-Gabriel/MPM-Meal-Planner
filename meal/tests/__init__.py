"""Test package for meal.
Having this file makes `meal.tests` an importable package so VS Code/unittest discovery
adds the project root (parent of `meal`) to sys.path instead of only `meal/tests`.
"""
